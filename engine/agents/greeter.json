{
  "name": "greeter",
  "description": "Sub-agent example for agent_call: echoes a greeting back. tool_call is currently a stub (see #15), so this demonstrates the agent_call wiring rather than real tool execution.",
  "output_step": "greet",
  "steps": [
    {
      "id": "greet",
      "type": "tool_call",
      "tool": "echo",
      "input_template": "Hello, {{user_input}}!",
      "depends_on": []
    }
  ]
}
